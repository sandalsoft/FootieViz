import Ember from 'ember';
import Store from 'footieviz/models/store';


export default Ember.Route.extend({
  model: function() {
      // var store = Store.create();
      // var model = {};
      // model.myTeam = store.find('owner', '53cff023d7bf060000850910');
      // // model.allPlayers = store.findAll('player');
      // return model;
  },
});
